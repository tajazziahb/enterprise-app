<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= meta.title %></title>
  <style>
    /* PDF-friendly page styling */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: #f3f4f6;
      color: #111827;
    }

    .page {
      width: 8.5in;
      min-height: 11in;
      margin: 24px auto;
      background: #ffffff;
      padding: 0.75in;
      box-shadow: 0 10px 30px rgba(0,0,0,0.12);
      box-sizing: border-box;
    }

    .doc-title {
      font-size: 22px;
      font-weight: 800;
      margin: 0 0 6px;
      letter-spacing: 0.2px;
    }

    .doc-subtitle {
      margin: 0 0 16px;
      font-size: 12px;
      color: #374151;
    }

    .meta {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px 18px;
      font-size: 12px;
      color: #374151;
      padding: 12px;
      border: 1px solid #e5e7eb;
      border-radius: 10px;
      margin-bottom: 18px;
    }

    .meta strong { color: #111827; }

    .content {
      font-size: 12.5px;
      line-height: 1.65;
      white-space: pre-wrap;
      word-wrap: break-word;
    }

    .footer {
      margin-top: 22px;
      padding-top: 12px;
      border-top: 1px solid #e5e7eb;
      font-size: 11px;
      color: #6b7280;
    }

    @page {
      size: Letter;
      margin: 0.5in;
    }
  </style>
</head>
<body>
  <div class="page">
    <h1 class="doc-title"><%= meta.title %></h1>
    <p class="doc-subtitle"><%= meta.subtitle %></p>

    <div class="meta">
      <div><strong>Business:</strong> <%= meta.businessName %></div>
      <div><strong>Industry:</strong> <%= meta.industryLabel %></div>
      <div><strong>Location:</strong> <%= meta.location %></div>
      <div><strong>Size:</strong> <%= meta.size %></div>
      <div><strong>Generated:</strong> <%= meta.generatedOn %></div>
      <div><strong>Doc Type:</strong> <%= meta.docTypeLabel %></div>
    </div>

    <div class="content"><%= content %></div>

    <div class="footer">
      Draft document generated by Document Generator. Review recommended before use.
    </div>
  </div>
</body>
</html>